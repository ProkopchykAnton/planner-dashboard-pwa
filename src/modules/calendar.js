export function initCalendar(targetId) {
  const el = document.getElementById(targetId);
  if (!el) return;
  el.textContent = 'Calendar module not implemented yet';
}

export function initCalendar(targetId) {
  const el = document.getElementById(targetId);
  if (!el) return;
  el.textContent = 'Calendar: not implemented yet';
}
